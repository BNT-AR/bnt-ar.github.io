import styled from 'styled-components'
import { bp } from '../../../core/styled/helpers'

export default styled.p`
  font: 400 18px/2 'calluna-sans', 'Helvetica Neue', Arial, sans-serif;
  ${bp('ts')`
    font-size: 16px;
  `}
`
